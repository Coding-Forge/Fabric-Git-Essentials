# Local Power BI Project artifacts (do not commit)
shared/**/*.pbip
shared/**/*.pbir
shared/**/*.pbism
shared/**/*.Report/
shared/**/*.SemanticModel/

# Keep local PBIP sandbox folder in repo while ignoring artifacts inside it
!shared/pbip-local/
!shared/pbip-local/README.md
